# If Hostinger document root is the PROJECT ROOT (parent of public/),
# send all traffic into /public. Prefer setting document root to /public instead.
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
